/*global
    $,
    normalise
*/


$.normaliseAm = normalise;

/**
 * Alias for [jQuery.normaliseAm]{@link external:jQuery.normaliseAm}.
 *
 * @memberof external:jQuery
 * @alias    external:jQuery.normalizeAm
 */
$.normalizeAm = normalise;
